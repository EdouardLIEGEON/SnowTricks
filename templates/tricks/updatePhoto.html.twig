{% extends 'base.html.twig' %}

{% block title %}Modifier la photo | {% endblock %}

{% block body %}

    <section id='tricks'>
        <h1>Modifier la photo</h1>
        <div class="container" >
            <div class="row">
                <div class="col form">
                        {{ form_start(UpdatePhoto) }}
                            {{ form_row(UpdatePhoto.photo)}}
                            <button type="submit" class="btn btn-lg btn-primary my-3">Modifier la photo</button>
                        {{ form_end(UpdatePhoto) }}
                </div>
            </div>
        </div>

    </section>
{% endblock %}